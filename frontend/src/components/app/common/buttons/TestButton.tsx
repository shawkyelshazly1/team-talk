"use client";
// import { increment } from "@/app/stores/features/counter/counterSlice";

export default function TestButton() {
	return (
		<button
			onClick={() => {
				// dispatch(increment());
			}}
		>
			{/* {count} */}
		</button>
	);
}
